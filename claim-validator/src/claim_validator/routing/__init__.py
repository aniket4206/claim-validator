"""Payer routing — resolves payer IDs to clearinghouse providers.

Public API::

    from claim_validator.routing.models import PayerRoute
    from claim_validator.routing.router import PayerRouter
"""
